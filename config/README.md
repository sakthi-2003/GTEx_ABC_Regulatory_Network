# Configuration Directory

Use this directory for project-level configuration files, such as path templates, analysis thresholds, dataset version metadata, and workflow parameters.

Recommended future files:

- `paths.example.yml`
- `analysis_parameters.yml`
- `dataset_versions.yml`
