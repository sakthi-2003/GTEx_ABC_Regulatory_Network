# Legacy Step-by-Step Scripts

The original incremental scripts were consolidated into the three main workflow scripts in `scripts/`.

Legacy scripts are retained locally for traceability, while the GitHub repository keeps the public workflow concise and professional.
